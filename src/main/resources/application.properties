spring.application.name=pawlife-backend
server.port=3000

# We will use SQLite for ease of development for now, matching the current fallback
spring.datasource.url=jdbc:sqlite:database/pawlife.db
spring.datasource.driver-class-name=org.sqlite.JDBC

# Uncomment to use MySQL in production
# spring.datasource.url=jdbc:mysql://127.0.0.1:3306/paw_life?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=nethula@2005
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Static resources
spring.web.resources.static-locations=classpath:/static/
